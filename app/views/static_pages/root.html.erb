<% if logged_in? %>
  <script type="text/javascript">
      window.currentUser = <%= render(
        "api/users/user.json.jbuilder",
        user: current_user
      ).html_safe %>
      window.userRelationships = <%= render(
        "api/user_relationships/rel.json.jbuilder",
        user: current_user
      ).html_safe %>
  </script>
<% end %>

<div id="root">
  This will be replaced by React.
<div>
